# 轻量级部署依赖 - 用于Railway部署
# 移除了大型AI依赖以减小Docker镜像大小

# 核心Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0

# 数据库
sqlalchemy==2.0.23

# HTTP客户端
httpx==0.25.2

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# 基础数据处理（轻量级版本）
numpy==1.26.2
python-dateutil==2.8.2

# 注意：AI功能暂时使用HTTP API调用而不是本地模型
# 这样可以大幅减小Docker镜像大小